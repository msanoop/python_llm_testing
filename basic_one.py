name = "Anoop M S"
age = 35
status = "Active"

print(name)
print(age)
print(status)


# ---------------------------------------------------------
# EXERCISE: 
# 1. Create a variable for an LLM response (string).
# 2. Create a variable for a response length (integer).
# 3. Print a message combining them using an f-string.
# ---------------------------------------------------------


response = "Hello, how are you?"
response_length = len(response)
print(response)
print(f"The response is {response_length} characters long.")
print(f"The response is {response_length} characters long.")



LLMResponse = "This is a test response from a LLM."
LLMResponseLength = len(LLMResponse)
print("the response is: "+ LLMResponse)
print("and the length is " + str(LLMResponseLength))
print(f"the length is {LLMResponseLength}")

